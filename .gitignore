# Local backups may contain personal data — keep private
data/*.json
data/tabel.db
data/tabel.db-wal
data/tabel.db-shm
!data/example-tabel.json
data/xlsx-cols.txt
data/xlsx-row-dump.txt

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets — never commit
.env
.env.*
!.env.example
fst-web/.env
fst-web/.env.production
fst-web/.env.target
fst-web/.env.vercel
fst-web/.env.production.vercel
fst-web/service-account*.json
.env.vercel.*
fst-web/dist
.firebase

# Vercel local link (recreated by vercel link)
.vercel

# Stale build artifacts
live-index.html

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# graphify knowledge graph (regenerated via: graphify update .)
graphify-out/

# Cursor chain-agent brief (regenerated via: npm run agent:chain-brief)
.cursor/context/CHAIN_BRIEF.md

# Android / Capacitor build outputs (project sources under fst-web/android are tracked)
fst-web/android/app/build/
fst-web/android/.gradle/
fst-web/android/build/
fst-web/android/local.properties
*.apk
*.aab
*.keystore
*.jks
# Публичный APK для скачивания с сайта
!fst-web/public/downloads/*.apk

# Firebase Android (нужен для сборки APK с пушами; ключ клиентский)
# fst-web/android/app/google-services.json — в репозитории для CI/сборки

.vercel
.env*.local

# R2.4 local staging SQL helpers (never commit)
cloud-sql-proxy.exe
tmp-stg-*.txt
tmp-stg-*.sql
tmp-access-token.txt
.env*
