import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.otgruzka.tovara.app',
  appName: 'Fibercell Manufacturing OS',
  webDir: 'dist'
};

export default config;
